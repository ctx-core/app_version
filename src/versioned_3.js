import { versioned_ } from './versioned_';
/**
 * _versioned with ctx
 * @param {*}ctx
 * @returns {string}
 */
export function versioned_3(params) {
    return function versioned_2(url) {
        return versioned_(url, params);
    };
}
//# sourceMappingURL=src/versioned_3.js.map